# typescript-eslint's peer range (<6.1.0) rejects the TypeScript 7 compiler this repo builds
# with; scripts/link-lint-typescript.mjs splices a TypeScript 6 copy in for the lint stack.
# Drop this together with that script once typescript-eslint supports TypeScript 7.
legacy-peer-deps=true
